# Legacy material

This directory contains historical analyses preserved for provenance.

The contents are not part of the maintained package and are not used by CI as modelling examples. They may depend on obsolete APIs, packages, or assumptions that no longer represent the direction of the project.

The original notebook is stored under `notebooks/`. Maintained statistical functionality should be implemented under `src/time_series_additive_models/` with tests and documentation.
